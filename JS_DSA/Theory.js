/* 
    in js switch default is not necessary.
    Math.sqrt() for square root
    
*/

let x = 'abc'; // this is a string literal
let a = new String('abc'); // this creates a string object

console.log(a == x);
console.log('abc' == new String('abc'));
